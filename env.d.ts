type UserServiceEnv = {
  ENVIRONMENT: string;

  BE_HOST: string;
  BE_PORT: number;

  // database
  DB_HOST: string;
  DB_PORT: number;
  DB_USER: string;
  DB_PASSWORD: string;
  DB_NAME: string;
};
